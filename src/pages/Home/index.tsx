import { Box, Typography } from "@mui/material";
import React from "react";

const HomePage = () => {
  return (
    <Box sx={{ backgroundColor: "red" }}>
      <Typography>Hi, First web deisgn</Typography>
    </Box>
  );
};

export default HomePage;
